# Project Instructions

## Git Branch Policy

- Always work directly on the `main` branch
- Never create feature branches
- After every change, commit to `main` and push immediately
- Use `mcp__github__push_files` for pushing (git push via HTTP returns 403)
